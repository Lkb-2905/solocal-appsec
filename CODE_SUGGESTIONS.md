# Suggestions de code (actions rapides)

- `subdomains.py`: ajouter un export CSV.
- `jwt_decode.py`: ajouter un mode `--pretty` pour affichage.
- `app.py`: ajouter un parametre `--limit`.
- `sqli_check.py`: ajouter un rapport JSON standardise.
- `log_parser.py`: ajouter un `--output` CSV.
