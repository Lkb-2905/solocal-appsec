## Validateur de tokens JWT

Objectif: decoder un JWT et afficher les claims.

Fonctions prevues:
- decodage base64url
- affichage payload
- gestion erreurs format

Livrables:
- script `jwt_decode.py`
